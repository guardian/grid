{
  "name": "scan-dyn-tables",
  "version": "1.0.0",
  "description": "A script to scan a series of dynamo tables in parallel for keys. Handy when extracting e.g. all image ids persisted in dynamo services.",
  "main": "scan-dyn-table-for-keys.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jonathon Herbert",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.615.0"
  }
}
