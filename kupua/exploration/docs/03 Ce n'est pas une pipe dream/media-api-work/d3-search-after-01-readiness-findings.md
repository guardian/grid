# D3 search-after: findings and amendment workplan

**Status: DONE — 17 September 2026 (agreed D3 amendment scope).** E1, E2, C1-C3 and
N4 coverage/disclosure are implemented, independently reviewed, validated and committed.
Completion commits: E2 `f092d854a`, C1 `1b4922fde`, and Scala `e6485be4b`.
The paired Kupua/docs amendments accompany this record. Section 7 records the local and
live verification and its limits.

Sections 1-6 preserve the original findings, decisions and completed execution plan;
their forward-looking wording is not a pending implementation queue. Explicitly deferred
findings remain deferred. PR-branch extraction and remote PR updates are separate operator
actions, not unfinished code in this batch.

**Original assessment:** 15 September 2026, report-only at that time.
**Original amendment plan:** 16 September 2026, following operator discussion,
historical-document checks and review of the public PR description.

## 1. Decision

**Required D3 amendment batch:** E1 on the server, plus required companion client fixes E2 (date parity), C1 (authoritative cursors), C2 (complete enrichment publication) and C3 (existing hybrid availability/PIT-expiry recovery). Client ownership does not make these optional improvements.

**Timing:** Plan, implement and validate client and server work in the same amendment period, following dependencies rather than an all-client-first or all-server-first rule. Scala-only commits are mandatory for porting to the PR branch; Kupua code, tests and documentation belong in separate commits. Required client fixes must not slip into an unspecified later phase. The matrix's "fix before review" classification covers coordinated amendment readiness, not a mixed Scala/client PR.

**Boundaries:** E2 changes the client to match existing D3/Grid dates, with a bounded scope check. E3 remains separate policy work; C3 restores current-mode recovery, not stronger snapshot guarantees. V1's optional strictness is not a blanket gate. Retain N4's shared rights filter and collection-sort corrections with explicit GET regression coverage and PR disclosure. This is the execution choice, not Grid-team approval or production certification.

## 2. Disposition Matrix

Evidence: **S** = current source; **T** = existing test assertion, read only; **H** = historical measurement; **O** = operator fact; **U** = unverified hypothesis. Confidence concerns the stated behavior, not its production frequency. Test names marked missing are proposed checks, not executed verification.

| Candidate | Owner | Evidence / Confidence | Classification | Concrete Consequence | Smallest Next Action / Disproof |
|---|---|---|---|---|---|
| E1. Deleted authorization and absent query | Endpoint | S/high: [media-api/app/controllers/MediaApi.scala:854](../../../../../media-api/app/controllers/MediaApi.scala#L854), [media-api/app/lib/elasticsearch/ElasticSearchModel.scala:156](../../../../../media-api/app/lib/elasticsearch/ElasticSearchModel.scala#L156). Missing D3 principal/default-query tests. | **fix before review** | Positive deleted searches lack uploader restriction; absent or wrong-typed `q` bypasses hiding defaults. Hits and totals can include other uploaders' deleted images. | Enforce parsed intent and default hiding before ES; disprove with ordinary-user versus privileged-user hits/totals fixtures. |
| E2. Six top-level date bounds | Client | S/high: [media-api/app/lib/elasticsearch/QueryBuilder.scala:133](../../../../../media-api/app/lib/elasticsearch/QueryBuilder.scala#L133), [common-lib/src/main/scala/com/gu/mediaservice/lib/elasticsearch/filters.scala:42](../../../../../common-lib/src/main/scala/com/gu/mediaservice/lib/elasticsearch/filters.scala#L42), [kupua/src/dal/es-adapter.ts:495](../../../../src/dal/es-adapter.ts#L495). O: client alignment chosen; boundary fixture missing. | **fix before review** | D3 already matches Grid's exclusive top-level bounds; direct Kupua includes boundary images. Neither convention is intrinsically correct. | Required companion work in the D3 amendment batch. Keep Scala/shared date helpers unchanged; align the six client bounds, starting at the shared query builder. Confirm scope and ask if it expands materially. |
| E3. Partial responses and invalid projected hits | Endpoint/client | S/high: [media-api/app/lib/elasticsearch/ElasticSearch.scala:858](../../../../../media-api/app/lib/elasticsearch/ElasticSearch.scala#L858), [kupua/src/stores/search-store.ts:1044](../../../../src/stores/search-store.ts#L1044). Missing timeout/shard/invalid-hit fixtures. | **team decision/disclosure** | This failure class also exists in Grid. In Kupua it can affect continued navigation: incomplete totals, skipped images or early fill termination. Accepted image/tuple pairs remain aligned. | Disclose now; address failure policy for new endpoints as separate bounded work. No blanket rejection behavior, global Grid repair or D3-only implementation is selected merely because D3 comes first. |
| V1. Remaining body validation | Endpoint | S/high: [media-api/app/lib/elasticsearch/ElasticSearchModel.scala:134](../../../../../media-api/app/lib/elasticsearch/ElasticSearchModel.scala#L134), [media-api/app/lib/elasticsearch/ElasticSearchModel.scala:142](../../../../../media-api/app/lib/elasticsearch/ElasticSearchModel.scala#L142), [media-api/app/lib/elasticsearch/ElasticSearchModel.scala:414](../../../../../media-api/app/lib/elasticsearch/ElasticSearchModel.scala#L414). Missing malformed-scalar tests. | **team decision/disclosure** | Wrong-typed PIT, direction, filters and integer fields silently default/disappear; negative lengths lack a deliberate lower-bound rejection. Some permissiveness is inherited; correct Kupua requests avoid these paths. | Preserve necessary authorization, cursor and work-limit safeguards. Judge additional D3 fixes by concrete need; record consistent strict rejection as a possible future target, not an instruction to harden all inputs or alter GET. |
| N1. Raw sort and PIT transport | Endpoint/client | S/high: [media-api/app/lib/elasticsearch/sorts.scala:39](../../../../../media-api/app/lib/elasticsearch/sorts.scala#L39), [media-api/app/lib/elasticsearch/ElasticSearch.scala:817](../../../../../media-api/app/lib/elasticsearch/ElasticSearch.scala#L817). U: additional cross-principal disclosure; no replay fixture. | **team decision/disclosure** | Sort fields are not allowlisted; unknown options are ignored, not forwarded. PITs are caller-supplied, unbound to Grid principal/query/sort, and renewed for one minute. | Keep Option B for this caller, document its supported subset and unique-ID/single-index preconditions. A foreign-PIT/changed-query fixture can test stronger security claims; wire shape alone does not require replacement. |
| N2. Projection, aliases and include | Endpoint | S/high: [media-api/app/lib/elasticsearch/ElasticSearch.scala:852](../../../../../media-api/app/lib/elasticsearch/ElasticSearch.scala#L852), [media-api/app/lib/ImageResponse.scala:323](../../../../../media-api/app/lib/ImageResponse.scala#L323). T: [media-api/test/lib/elasticsearch/ElasticSearchTest.scala:1432](../../../../../media-api/test/lib/elasticsearch/ElasticSearchTest.scala#L1432). | **team decision/disclosure** | Alias leaves survive; usage/collection arrays remain complete. `include=fileMetadata` expands the empty parsed default, not full metadata. Runtime alias configuration must match. | Retain lean projection and existing writer. Recommend explicit include rejection if the team closes this contract now; test that rejection, not a full-writer redesign. |
| C1. Authoritative client tuples | Client | S/high: [kupua/src/stores/search-store.ts:2557](../../../../src/stores/search-store.ts#L2557), [kupua/src/lib/image-offset-cache.ts:73](../../../../src/lib/image-offset-cache.ts#L73), [kupua/src/components/ImageGrid.tsx:681](../../../../src/components/ImageGrid.tsx#L681). Missing API-alias retention fixture. | **fix before review** | Eviction, range and history paths reconstruct raw-field cursors from API images. Configured `fileMetadata` aliases become false null primaries despite valid server tuples. | Required companion work: retain tuples by image ID and prefer them in those consumers. Exercise both eviction directions, range and restore using API-shaped alias images. |
| C2. Complete enrichment publication | Client | S/high: [kupua/src/stores/search-store.ts:1420](../../../../src/stores/search-store.ts#L1420), [kupua/src/stores/search-store.ts:1385](../../../../src/stores/search-store.ts#L1385), [kupua/src/stores/search-store.ts:2950](../../../../src/stores/search-store.ts#L2950), [kupua/src/stores/search-store.ts:3453](../../../../src/stores/search-store.ts#L3453). Missing publication fixtures. | **fix before review** | Fallback pages lose overlays; focus/restore omit the inserted probe target's overlay; both bidirectional seek paths omit backward overlays. Costs, validity and actions can be stale or absent. | Required companion work: carry every committed image's overlay and test distinct target/backward/fallback values. Discarded probes must remain side-effect free. |
| C3. API absence and PIT expiry | Client | S/high: [kupua/src/dal/grid-api-search-adapter.ts:138](../../../../src/dal/grid-api-search-adapter.ts#L138), [kupua/src/dal/strangler-adapter.ts:64](../../../../src/dal/strangler-adapter.ts#L64), [kupua/src/dal/es-adapter.ts:1054](../../../../src/dal/es-adapter.ts#L1054). Missing API-failure fixtures. | **fix before review** | API failure throws into search/extend error state; direct mode instead retries expired PITs without a PIT. Hybrid graceful absence is incomplete. | Required companion work limited to existing hybrid availability/expiry recovery. Preserve cancellation and access/validation refusals; do not import E3's deferred partial-result policy or a new session architecture. |
| N3. PIT timing and bookkeeping | Client | S/high: [kupua/src/stores/search-store.ts:2202](../../../../src/stores/search-store.ts#L2202), [kupua/src/stores/search-store.ts:1001](../../../../src/stores/search-store.ts#L1001), [kupua/src/stores/search-store.ts:2728](../../../../src/stores/search-store.ts#L2728). T: [kupua/src/stores/search-store-pit.test.ts:81](../../../../src/stores/search-store-pit.test.ts#L81). | **team decision/disclosure** | Page one runs outside the parallel-opened PIT in both modes. Scala returns refreshed IDs; backward extension/fill do not retain them. These are existing client limitations, not migration findings. | Disclose split snapshots. Include only bookkeeping needed for required C3 recovery; wider renewal/cleanup work remains separate. Preserve tested open-failure isolation; no durable-session prerequisite. |
| C4. Initial counts and count intent | Client | S/high: [kupua/src/dal/grid-api-search-adapter.ts:106](../../../../src/dal/grid-api-search-adapter.ts#L106), [kupua/src/stores/search-store.ts:2229](../../../../src/stores/search-store.ts#L2229), [kupua/src/dal/es-adapter.ts:703](../../../../src/dal/es-adapter.ts#L703). Missing count-intent wire test. | **later work** | Cursorless API probes/End recount despite absent count intent. Initial page and ticker request both count exactly; the store consumes page total and ticker subcounts, not the second total. | Map `countAll` from explicit intent; retain exact first-page total. Decide initial-count fusion/ownership with D7, using measurements. |
| N4. Existing Grid clients | Shared | S/high, including pre-D3 Git comparison: [media-api/app/controllers/MediaApi.scala:89](../../../../../media-api/app/controllers/MediaApi.scala#L89), [media-api/app/lib/elasticsearch/QueryBuilder.scala:160](../../../../../media-api/app/lib/elasticsearch/QueryBuilder.scala#L160), [kahuna/public/js/services/api/media-api.js:63](../../../../../kahuna/public/js/services/api/media-api.js#L63), [media-api/app/lib/elasticsearch/sorts.scala:22](../../../../../media-api/app/lib/elasticsearch/sorts.scala#L22). Missing GET rights-filter regression tests. | **team decision/disclosure** | GET already advertised `hasRightsAcquired` but ignored it; the current shared implementation honors it. Kahuna supports it through search URLs; usage frequency is unverified. Both collection-sort directions carry missing-mapping guards, alongside the acknowledged ascending-token support. | Retain the shared filter and documented sort corrections. Add omitted/true/false and missing/mixed-rights coverage, preserve existing status-filter semantics, and disclose the intentional GET behavior change. Do not create a D3-only exception. |
| N5. Containment and cost | Shared | S: [media-api/app/lib/elasticsearch/ElasticSearch.scala:84](../../../../../media-api/app/lib/elasticsearch/ElasticSearch.scala#L84), [media-api/app/lib/elasticsearch/ElasticSearchModel.scala:398](../../../../../media-api/app/lib/elasticsearch/ElasticSearchModel.scala#L398). H: [kupua/exploration/docs/03 Ce n'est pas une pipe dream/media-api-work/d3-search-after-04-performance.md:222](d3-search-after-04-performance.md#L222). High on measured topology; production capacity unverified. | **team decision/disclosure** | One ES search per D3 request, 200-hit ceiling and 10s query timeout; no new backend cursor-walk loop. Requests still share JVM, ES and signing resources; caps do not bound aggregate load. | Reuse existing measurements for team capacity/enablement decisions. Neither a new writer nor a deployed-D3 load campaign is required just to return to review. |
| R1. Actual-client defaults and filters | Endpoint/client | S/high: [kupua/src/dal/grid-api-search-adapter.ts:95](../../../../src/dal/grid-api-search-adapter.ts#L95), [media-api/app/lib/querysyntax/Parser.scala:10](../../../../../media-api/app/lib/querysyntax/Parser.scala#L10). T: [media-api/test/lib/querysyntax/ParserTest.scala:15](../../../../../media-api/test/lib/querysyntax/ParserTest.scala#L15). | **already resolved/refuted** | Client injection and parser guards are idempotent for emitted requests, not double filtering. Rights/crops/free/uploader/date/ID/status mappings exist; `payType`/`persisted` remain current-client no-ops. Absent `q` is E1. | Keep mappings; add a focused body-to-query parity fixture when changing this boundary. No general parser rewrite inferred. |
| R2. Previous sort/cursor findings | Endpoint | S/high: [media-api/app/lib/elasticsearch/ElasticSearch.scala:748](../../../../../media-api/app/lib/elasticsearch/ElasticSearch.scala#L748). T: [media-api/test/lib/elasticsearch/SortsTest.scala:10](../../../../../media-api/test/lib/elasticsearch/SortsTest.scala#L10), [media-api/test/lib/elasticsearch/ElasticSearchTest.scala:1116](../../../../../media-api/test/lib/elasticsearch/ElasticSearchTest.scala#L1116), [media-api/test/lib/elasticsearch/ElasticSearchTest.scala:1196](../../../../../media-api/test/lib/elasticsearch/ElasticSearchTest.scala#L1196). H: [kupua/exploration/docs/zz Archive/media-api-work/phase-3-d3-searchafter-post-pr-review.md:108](../../zz%20Archive/media-api-work/phase-3-d3-searchafter-post-pr-review.md#L108). | **already resolved/refuted** | Known shape/order/mode, offset, duplicate/special-alias, scalar/arity/residual-null, nested-null and terminal-sentinel fixes exist. Runtime-mapping parity is covered. `_shard_doc` truncation has a measured successful unique-ID walk. | Preserve these assertions and the public tuple length; do not resurrect the reverted tiebreaker proposal. |

E1 can reuse the auth and permission service already supplied to D3 ([media-api/app/MediaApiComponents.scala:34](../../../../../media-api/app/MediaApiComponents.scala#L34)). Use parsed positive intent, not legacy GET's exact-string `Option.contains` predicate ([media-api/app/controllers/MediaApi.scala:573](../../../../../media-api/app/controllers/MediaApi.scala#L573)). Scope both hits and counts before ES. `isVisibleToAccessor` admits Internal principals and alone cannot fix this ([media-api/app/controllers/MediaApi.scala:159](../../../../../media-api/app/controllers/MediaApi.scala#L159)). Keep ReadOnly/Syndication keys' existing POST denial ([common-lib/src/main/scala/com/gu/mediaservice/lib/auth/ApiAccessor.scala:29](../../../../../common-lib/src/main/scala/com/gu/mediaservice/lib/auth/ApiAccessor.scala#L29)); no separate reachable syndication-tier disclosure is established.

E2's chosen correction is client-side: use exclusive `gt`/`lt` for the six top-level bounds, consistently across search and companion operations. Do not create a second Scala endpoint with a different date convention. Check the implementation/test footprint first; ask before a wider change. CQL date expressions already match Grid's inclusive convention and stay unchanged ([media-api/test/lib/elasticsearch/QueryBuilderTest.scala:90](../../../../../media-api/test/lib/elasticsearch/QueryBuilderTest.scala#L90)).

E3 is inherited: legacy GET logs timeouts and accepts returned hits; direct Kupua also omits partial-status checks ([media-api/app/lib/elasticsearch/ElasticSearch.scala:428](../../../../../media-api/app/lib/elasticsearch/ElasticSearch.scala#L428), [kupua/src/dal/es-adapter.ts:1008](../../../../src/dal/es-adapter.ts#L1008)). Cursor-driven navigation can amplify the consequences, but comparative severity/frequency was not measured. Rejecting incomplete data trades misleading success for request failure; that policy is not yet chosen. Separate work should establish reusable expectations for new endpoints, distinguishing complete pages/exact counts from optional enrichment, rather than repeatedly inventing fixes or retrofitting Grid. It need not wait until every endpoint is built, and does not authorize a universal failure framework.

V1 should address concrete D3 needs, not pursue perfection. An ideal future API might consistently reject malformed input with clear errors; that is a design target, not an approved implementation gate. Existing Grid tolerance and defects can be documented without changing its parser. This does not defer E1 or weaken existing cursor and work-limit safeguards.

**N4 historical check:** the [12 June F-3 decision](../../zz%20Archive/media-api-work/phase-3-d3-searchafter-code-review.md#L231) identifies a URL/syndication-workflow filter and requires mapping it through; the [final review](../../zz%20Archive/media-api-work/phase-3-d3-searchafter-code-review-final.md#L54) confirms that implementation. The [build worklog](../../zz%20Archive/media-api-work/phase-3-d3-searchafter-worklog.md#L196) already records both collection-sort guards. These support intent, not measured usage or prior human approval of GET changes. Current code decides semantics: `true` means any acquired right; `false` means none, including missing information. Do not equate the latter with `syndicationStatus=unsuitable`, whose existing [mixed-rights behavior](../../../../../media-api/app/lib/elasticsearch/SyndicationFilter.scala#L22) is unchanged.

The [changelog's `nonFree` mapping shorthand](../../changelog.md#L2060) is inaccurate: [source](../../../../src/dal/grid-api-search-adapter.ts#L126) maps it to `free`, separately from `hasRightsAcquired`. Other inventories and the archived API-only assessment corroborate parameter coverage, not current usage or authority to implement their broader architecture.

## 3. Fix/Test Impact

Every defect fix needs a test confirmed failing for that reason, followed by the full relevant surface. N4 verifies retained behavior: its added contract tests may already pass; do not manufacture a failure or claim a new fix. No product tests were run here.

- **E1:** Add controller/auth fixtures for privileged user, own uploader, other ordinary user, rejected machine tiers, and positive/compound/negative/empty/absent queries. Assert hits and exact totals together. Existing parser negations and [media-api/test/lib/elasticsearch/ElasticSearchTest.scala:494](../../../../../media-api/test/lib/elasticsearch/ElasticSearchTest.scala#L494) tier assertions stay unchanged; they do not exercise controller uploader scoping.
- **E2, required companion implementation:** Add before/at/after fixtures for all six bounds; require Kupua, D3 and GET to agree on exclusion at equality. Identify and update client assertions expecting inclusive top-level bounds as an intentional behavior change. Preserve CQL-inclusive assertions and consistent search/count/navigation behavior. No Scala date change is selected.
- **E3, separate policy work:** Use timeout, failed-shard and invalid-hit fixtures, including an all-invalid page, to characterize current behavior and test the eventual decision. Do not preselect rejection assertions. Retain valid alias projection and image/tuple pairing coverage; do not change GET's outcomes without separate approval.
- **N4, retained shared behavior:** Cover GET/D3 with the flag omitted, true and false; include missing, empty, all-false, all-true and mixed rights records. Omission must preserve existing results; true matches any acquired right and false matches none. Keep existing `syndicationStatus` behavior and pin both collection-sort directions/guards. Do not infer normal UI usage from URL support.
- **V1/N2, no broad change selected:** Add only guards needed for a required case, with a stated reason; preserve [media-api/test/lib/elasticsearch/ElasticSearchTest.scala:778](../../../../../media-api/test/lib/elasticsearch/ElasticSearchTest.scala#L778) valid cursor-body cases. Leave legacy status-enum behavior outside this work: [common-lib/src/main/scala/com/gu/mediaservice/model/usage/UsageStatus.scala:20](../../../../../common-lib/src/main/scala/com/gu/mediaservice/model/usage/UsageStatus.scala#L20), [common-lib/src/main/scala/com/gu/mediaservice/model/SyndicationStatus.scala:16](../../../../../common-lib/src/main/scala/com/gu/mediaservice/model/SyndicationStatus.scala#L16).
- **C1:** Extend [kupua/src/stores/search-store-eviction-cursor.test.ts:104](../../../../src/stores/search-store-eviction-cursor.test.ts#L104) with API-only alias fields plus authoritative tuples. Existing extractor-null tests may stop exercising their fallback once tuples are retained: explicitly preserve fallback coverage, not merely non-null assertions. Add range/history/focus-trim cases; do not change exact-coordinate expectations.
- **C2:** Add distinct overlays for fallback, probe target and backward seek pages, plus abort/nonpublication cases. Preserve [kupua/src/dal/grid-api-search-adapter.test.ts:320](../../../../src/dal/grid-api-search-adapter.test.ts#L320) probe immutability and [kupua/src/stores/search-store.test.ts:576](../../../../src/stores/search-store.test.ts#L576) fallback focus/position assertions. Fresh search, fill and ordinary extensions already publish overlays; fix the omitted contributions, not their successful semantics.
- **C3, required current-mode recovery:** Add network/API-absence/expiry/abort fixtures and preserve [kupua/src/dal/strangler-adapter.test.ts:20](../../../../src/dal/strangler-adapter.test.ts#L20) shallow direct-offset routing. Recovery must not bypass authorization or deliberate refusals. This does not select E3's future partial-result policy.
- **C4/N3, separately deferred scope:** Add count-intent and rotated-PIT fixtures when addressing those findings. Preserve [kupua/src/stores/search-store-pit.test.ts:163](../../../../src/stores/search-store-pit.test.ts#L163) superseded-PIT cleanup; do not turn stronger snapshot guarantees into a companion-work prerequisite.

Required future gates: full `TZ=UTC sbt "media-api/test"` for Scala changes; `npm --prefix kupua test` for Kupua source changes; habitual `npm --prefix kupua run test:e2e` for store/hook/component/scroll changes. Before Playwright, warn and confirm ports 3000/3030 are free; stream foreground runs through `tee` with `pipefail`. Cursor/sort changes additionally require the guarded [kupua/integration/special-sort-es.test.ts:1](../../../../integration/special-sort-es.test.ts#L1) local oracle, never against TEST/PROD. Suggest applicable performance checks after render/store timing changes; do not run them automatically.

## 4. PR Description Copy

**Editorial note, not part of the copy:** the block below describes the completed amendment batch. Do not present the planned E1/client fixes as implemented before their tests pass. Keep this copy here while changes settle; after the approved commit(s), reconcile the [existing PR-description document](d3-search-after-02-pr.md#L1), then the operator copies it to [PR #4849](https://github.com/guardian/grid/pull/4849). The current PR already correctly discloses ascending collection sorting; replace its blanket GET/Kahuna-unchanged claim, not the acknowledged sort decision. Add actual tested refs, commands and outcomes to Validation; do not reuse historical green counts as fresh results.

```markdown
### Containment

D3 applies Grid's existing uploader/permission rule for deleted searches to both hits and totals. Legacy GET authorization is unchanged.

Two intentional shared corrections are retained:
- GET /images now honors its already-advertised hasRightsAcquired parameter. True matches any acquired right; false matches none, including missing information. Requests omitting it are unchanged. Kahuna supports the parameter in search URLs, so those searches can return different results/counts. Existing syndicationStatus semantics are unchanged.
- Ascending Added to collection is supported for direct GET callers, with unmapped-field guards on both collection-sort directions. Kahuna still normalizes the ascending token to Uploaded; its UI sorting behavior is unchanged.

Existing createSort, GET date conventions and ImageResponse shaping are retained.

### Companion Kupua Changes

Paired client amendments align all six top-level date bounds with Grid/D3's exclusive convention, retain response cursors through paging/range/history, preserve enrichment for every committed API image, and restore existing hybrid availability/PIT-expiry recovery. CQL date expressions retain their existing inclusive semantics. Direct-ES and hybrid development modes remain supported; this is not API-only completion.

### Limits and Review Points

Index migration is unsupported. Page one remains outside the parallel-opened PIT. Partial-result policy, broader malformed-input strictness and the lean projection's include=fileMetadata contract remain disclosed follow-up questions, not a new session architecture. POST-for-reads and the shared corrections remain team review points.

Local-media-api performance campaigns and the separate deployed-TEST GET gzip experiment are not a deployed-D3 load campaign or a production-capacity verdict.
```

**Evidence for the copy:** existing measurements cover [373KB raw/70KB gzipped source per 200 hits](d3-search-after-04-performance.md#L120), [137ms envelope including 29ms signing](d3-search-after-04-performance.md#L222), and [28ms large-page exact-count overhead](d3-search-after-04-performance.md#L197). These are historical, not new guarantees. [Compression already shipped](../../../../../common-lib/src/main/scala/com/gu/mediaservice/lib/elasticsearch/ElasticSearchClient.scala#L50); preserve the [topology qualifications](../../../../e2e-perf/README.md#L121). No writer rewrite or capacity campaign is required merely for review.

## 5. Coverage Limits

Inspected working-tree code on `mk-next-next-next`, HEAD `2232c9193806922d599faae01c2ad468be505f13`, using the user-owned current documentation. Compared named D3 Scala paths with local `mk-api-1of9-searchAfter` and its origin-tracking ref at `4fb75e33c`; the D3 implementation matches, although the whole controller differs elsewhere. No Git refs were fetched. The public PR description and listed commit history were read on 16 September; that does not verify deployed production code or test results.

One laptop caller, one successful TEST deployment, draft status and no human review are operator facts from [kupua/exploration/docs/03 Ce n'est pas une pipe dream/media-api-work/media-api-00-index.md:10](media-api-00-index.md#L10), not independently verified runtime observations. Product tests, builds, profilers and application/ES/AWS requests were not executed. Historical green counts are not current verification; habitual E2E normally blocks API calls ([kupua/e2e/shared/helpers.ts:27](../../../../e2e/shared/helpers.ts#L27)). Actual failure frequency, effective runtime configuration and sustained production capacity remain unverified. All named candidates are disposed of; no broader audit or architecture programme is proposed.

## 6. Execution

**Start gate:** begin only on an explicit implementation request. Follow the repository's fresh-agent protocol and obtain explicit permission to edit media-api outside kupua. Inspect current worktree/ref differences and preserve unrelated edits. This plan does not authorize live Grid/ES/AWS requests, remote fetches, branch switches, staging, commits or pushes. Local test execution belongs to the authorized implementation task, not this documentation pass.

**Frozen scope:** E1, E2, C1-C3 and N4 contract coverage/disclosure. Retain shared hasRightsAcquired behavior and the documented collection-sort corrections. Preserve legacy GET behavior otherwise. Leave E3 policy, broad V1 hardening, include expansion/rejection, C4 count ownership/fusion and wider N3 lifecycle work separate. No new sort builder, writer, endpoint programme, migration support, durable sessions or shared failure framework. Source outranks historical prose; the active index and this agreed scope supersede archived execution plans.

Use this dependency-led sequence with Section 3's tests and gates. Each defect gets a confirmed failing test before its smallest fix; retain focused and full required checks for each completed slice. Group manual/live checks by app mode where practical, not by changing the implementation scope or skipping regression gates.

1. **E2, client dates first.** Start at the shared client query builder. Align six top-level bounds with existing exclusive Grid/D3 semantics across search/count/positional callers; no new Scala behavior is needed. Keep CQL date semantics unchanged. Confirm the footprint first and ask before materially wider changes. Update inclusive-boundary assertions deliberately and run the required client checks.
2. **C1, shared client cursors.** Retain response tuples by image ID and search identity with bounded storage. Use them through commit, eviction, focus trimming, range and history; prefer authoritative tuples over raw-field reconstruction. Preserve direct-mode fallback and public tuple length. This can precede Scala amendments, but the confirmed alias bug is D3-specific: use API-shaped fixtures as well as direct-mode tests. Run units, habitual E2E and the guarded local cursor oracle; retain explicit D3 validation for the final combination.
3. **E1/N4 and required C3 support, server.** Establish the pre-D3 GET baseline and retained exceptions. Add E1 principal/query/default fixtures, then enforce authorization before D3 hits/totals using existing auth. Leave legacy GET authorization and machine method policy unchanged. Add N4 contract coverage without manufacturing failures. Establish any minimal D3-local expiry classification needed by C3, keeping legacy/shared error handling unchanged; do not make the client guess the new response contract. Run the full media-api suite.
4. **C2, D3 enrichment.** Carry fallback-page, inserted-target and backward-page contributions to publication points. Preserve replace-versus-merge semantics and probe/abort nonpublication; direct mode must not invent API enrichment. D3 already supplies the data, so this step may be implemented earlier using mocks without waiting for server amendments. Avoid a broad store/render refactor. Run units and habitual E2E, then verify the completed behavior in D3 mode.
5. **C3, recovery against the established contract.** Restore bounded hybrid availability/expired-PIT recovery without changing mode, bypassing refusals or treating every failure as absence. Preserve cancellation and clear expired IDs after live retry. Coordinate with step 3 if a server adjustment is needed, then rerun affected checks. Include only adjacent PIT bookkeeping needed for recovery, not E3's partial-result policy or stronger snapshot guarantees. Run the applicable full server/client surfaces.

**Mode validation:** test the final combined code in direct-ES and D3/hybrid modes. Group manual/live checks into mode-specific sessions to minimize switching, under the existing port warnings and per-session live-service permissions. Habitual E2E normally blocks API calls: a green run is direct-mode regression evidence, not proof of D3 integration. API-shaped mocks likewise do not replace explicit D3 integration checks. A blocked required check must be reported, not silently omitted.

**Commit boundary:** implementation may alternate between client and server as dependencies require; commits must not mix them. After validation and explicit commit approval, keep all Scala amendments in ONE self-contained media-api-only code/test commit for porting, with no Kupua code, tests or documentation. Put Kupua code/tests and documentation, including backend-change notes, in separate commit(s). Preserve and exclude the session worklog as explicitly requested by the operator. Inspect exact file lists before porting; do not blindly copy shared files or port the whole working branch. This section does not authorize commits, branch changes or porting.

**Done:** all five required findings and N4 coverage pass their applicable checks; the final combination has direct and D3-mode validation, with Scala-only porting boundaries verified. Do not claim whole-hybrid permission consistency without checking remaining direct-ES companions. If a finding is disproved, a required check cannot run, or preservation requires broader policy/infrastructure changes, stop with the concrete blocker rather than expanding scope or weakening assertions.

**Handoff:** record actual refs, commands, results and tested modes. Update the required [agent snapshot](../../../../AGENTS.md#L1) and owning guides/deviations; update the [changelog](../../changelog.md#L1) at commit boundaries. Preserve the worklog and exclude it from commits (operator override of the usual reset rule). After approved commit(s), reconcile Section 4 with final code and update the existing PR-description document. The operator updates GitHub. Do not create another workplan or edit archived assessments.

## 7. Amendment Verification (17 September 2026)

E1/N4, C2 and C3 were validated together based on C1 `1b4922fde` and E2 `f092d854a`.
The five media-api code/test files are isolated in Scala-only commit `e6485be4b`; client
amendments and this documentation are separate. PR-branch extraction has not been performed.
Independent written-code reviews found additional query-intent,
cancellation and expiry races; each retained finding was reproduced before its fix. Final
follow-up review accepted the repairs. The source-only restore-rank concern remains separate.

All commands ran from repository root, unsandboxed, with
`set -o pipefail; ... 2>&1 | tee "$TMPDIR/kupua-test-output.txt"`:

| Surface | Command | Result |
|---|---|---|
| Full Scala | `TZ=UTC sbt "media-api/test"` | 305 passed, 13 suites |
| Client build | `npm --prefix kupua run build` | TypeScript and Vite passed; non-fatal Vite warnings remain |
| Full units | `npm --prefix kupua test` | 1,342 passed, 69 files |
| Habitual E2E | `npm --prefix kupua run test:e2e` | 210 passed, 4.5 minutes |
| Guarded local oracle | `KUPUA_LOCAL_ES_MUTATION_OK=1 npm --prefix kupua run test:special-sort-es` | Passed against runner-managed loopback 9220 |

Scala fixtures exercise GET/D3 handlers over local ES for deleted hits and exact totals,
safe defaults, rights omission/true/false including mixed records, existing status semantics,
and all six exclusive date boundaries including UTC date-only inputs. Providers and image
serialization are mocked in those fixtures; existing sort/parser/response tests also passed.
The closed-PIT fixture returns HTTP 410 with `search-after-pit-expired`; malformed PITs are not
misclassified. Client tests cover publication, bounded recovery, refusal preservation and
PIT clearing across empty pages, concurrent responses, failed pairs and delayed commits.

**Live evidence, explicitly authorized read-only:** the pinned Dublin query returned 13,260
matches in both direct-ES and media-api modes. Direct-mode mapped paging, detail traversal,
reload/return and sort-around-focus preserved visible identity, retained tuples and coordinates.
In media-api mode, the initial page and both mapped seek requests returned 200; all 298 committed
seek images were enriched. Sort-around-focus refreshed the inserted target's overlay; reload/
restore retained enrichment; missing-target fallback replaced the map with the fresh 200-image
page. A >65k seek over 1,228,619 results committed 300 enriched images with consistent coordinates
and tuples, exercising the sequential backward-page path.

A PIT retired by normal app search was replayed through the live Strangler: HTTP 410 was observed,
and recovery returned two enriched hits with `pitId: null`, unchanged public cursor length and no
probe publication. The browser's full resource-timing buffer prevented an exact retry-count
measurement for that probe; the bound is covered by unit tests. A separate one-request browser
network abort verified live ES fallback without changing adapter mode or publishing probe data;
this is **fault-injected availability evidence**, not a naturally observed API outage.

**Limits:** no production deployment, capacity measurement or performance campaign; no live
multi-principal authorization matrix or new live special-sort/alias campaign. Those contracts
are covered by the stated local fixtures, not claimed as live evidence. Remaining direct-ES
companions still prevent a whole-hybrid permission-consistency or API-only completion claim.
Option B, deliberate `_shard_doc` truncation and the deferred E3/C4/wider-lifecycle scope remain.