# Config used in production should be placed in the file below
include "/etc/grid/collections.conf"

# Config used whilst running locally can also be placed in ~/.grid/collections.conf
# You can generate these files using the `generate-config` script in the `scripts` folder

aws.region=""
domain.root=""
panda.bucket.name=""
panda.settings.key=""
panda.user.domain=""
auth.keystore.bucket=""
dynamo.table.collections=""
dynamo.table.imageCollections=""
sns.topic.arn=""
thrall.kinesis.stream=""

play.application.loader = "AppLoader"
play.application.langs = "en"
play.http.session.secure = true
