server {
    listen 80;
    server_name elasticsearch.foo.co.uk;

    location / {
        proxy_pass http://elasticsearch:9200;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_redirect default;
    }
}
